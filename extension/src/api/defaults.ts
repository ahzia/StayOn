/**
 * Shipped default API URL — set before publishing VSIX / marketplace release.
 * Users can override via `stayon.apiBaseUrl` in settings; empty = no backend.
 *
 * After deploying `web/` (Vercel etc.), set this to your public URL, e.g.
 * `https://stayon.vercel.app`
 */
export const BUNDLED_API_BASE_URL = '';
